[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/admin"
  to = "/admin/index.html"
  status = 200

# Netlify Identity redirect for CMS auth
[[redirects]]
  from = "/"
  to = "/index.html"
  status = 200
  conditions = {Role = ["admin", "editor"]}
